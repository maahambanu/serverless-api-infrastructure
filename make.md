fmt:
```
	cd terraform/environments/staging && terraform fmt -recursive
```

validate:
```
	cd terraform/environments/staging && terraform validate
```

test:
```
	cd app && npm test
```

lint:
```
	cd app && npm run lint
```
